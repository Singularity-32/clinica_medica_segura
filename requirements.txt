# Sistema de Agendamento Clínico Seguro
# Python 3.12+ | Django 5.x

Django==5.1.4

# Banco de dados (req 3.4 / PostgreSQL)
psycopg2-binary==2.9.9

# Hash seguro de senha — Argon2 como fallback (req 1.1 / 1.2 / 1.3)
argon2-cffi==23.1.0

# Autenticação de dois fatores TOTP (req 1.5 / 1.6 / RNE02)
django-otp==1.5.1
qrcode==7.4.2
Pillow==10.4.0          # necessário para gerar QR em PNG

# Proteção brute force — rate limit (req 1.11 / RNF02)
django-axes==6.4.0

# Variáveis de ambiente (boas práticas)
python-decouple==3.8

# Testes de SCA (Trivy)
Django==4.0.0